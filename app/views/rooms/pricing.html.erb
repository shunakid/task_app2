<div class="row">
  <div class="col-md-3">
    <%= render 'room_menu' %>
  </div>
  <div class="col-md-9">
    <div class="panel panel-default">

      <div class="panel-heading">
        料金
      </div>

      <div class="panel-body">
        <div class="container">
          <%= form_for @room do |f| %>

            <div class="row">
              <div class="form-group">
                <label></label>
                <%= f.text_field :price, placeholder: "一泊あたりの料金を入力して下さい", class: "form-control", required: true %>
              </div>
            </div>

            <div class="text-center">
              <%= f.submit "決定", class: "btn btn-form" %>
            </div>

          <% end %>
        </div>
      </div>

    </div>
  </div>
</div>
